const numbers = [1, 2, 3, 4, 5];

/**
 * Exercicio:
 * 
 * 1. Cria um novo array numa constante "doubles"
 * 2. Utilizando o método map, itera pelos valores do array
 * "numbers"
 * 3. Obtém os seus valores multiplicados por 2
 * 
 */

function double(x, 2x) {
    return { x, 2x }
}

let doubles = [
    newdoubles(1,2),
    newdoubles(2,4),
    newdoubles(3,6),
    newdoubles(4,8),
    newdoubles(5,10),

]

function x2x(doubles) {
    return doubles.x + "vezes 2 é";
}

console.log(doubless.map(x2x));

/**
 * Exercicio:
 * 
 * 1. Cria um novo array numa constante "mapObject"
 * 2. Utilizando o método map itera pelo array "doubles"
 * 3. Cria um array de objectos em que cada objecto tem uma
 * chave "number" para representar o número.
 * ex. { number: 2 }
 * 
 */